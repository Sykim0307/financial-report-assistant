# 개발현황: FinDecode (금융 리포트 이해 보조 어시스턴트)

## 결론 (현재 상태)

`index.html`/`style.css`/`app.js` 3파일 구조 유지. **텍스트/PDF 입력 → Supabase Edge Function(analyze-report) 경유 Claude 분석 → 결과 표시 → localStorage 히스토리 + Supabase DB 저장**까지 엔드투엔드로 동작한다. 근거성(hallucination) 검증은 아직 없다.

프로젝트명을 "FinDecode"로 변경(구 명칭: 금융 리포트 이해 보조 어시스턴트).

## 태스크 현황

| 태스크 | 상태 | 비고 |
|---|---|---|
| 1. 화면 뼈대 | ✅ 완료 | 입력/결과/히스토리 3화면 + 내비게이션 |
| 2. localStorage 데이터 모델 + 히스토리 | ✅ 완료 | 최근 30건 캡, 원문은 20,000자까지만 저장(용량 보호) |
| 3. Claude API 연동 (텍스트만) | ✅ 완료 | 브라우저 fetch 직접 호출, 프롬프트로 JSON 출력 강제 |
| 4. 근거성 검증 (JS) | ⏳ 대기 | source_quote가 원문의 실제 부분문자열인지 검증 필요 |
| 5. 결과 화면 렌더링 | ✅ 완료 (3번에 포함) | 요약/키워드카드/인사이트뱃지 |
| 6. 엔드투엔드 확인 | ⏳ 대기 | 사용자 브라우저 테스트 대기 중 |
| PDF 업로드 지원 | ⏳ 보류 | 순수 JS는 PDF 파싱 불가 → CDN 라이브러리(pdf.js 등) 추가 여부 결정 필요 |

## 아키텍처 결정 로그

- **2026-08-12**: 최초 계획은 FastAPI 백엔드 + React 프론트 + SQLite였으나, 사용자 지시로 **순수 HTML/CSS/JS 3파일 + localStorage**로 전면 전환. 이유: 빌드 도구/서버 없이 가볍게 유지.
- **API 키 처리**: 백엔드가 없어 Claude API를 브라우저에서 직접 호출(`anthropic-dangerous-direct-browser-access` 헤더 사용). API 키는 사용자가 입력 화면에 직접 입력해 `localStorage`에 평문 저장됨. **주의**: 이 앱을 공개 배포하면 API 키가 열람될 수 있으므로, 로컬/개인용으로만 사용할 것.
- **JSON 출력 방식**: Claude의 구조화 출력 기능(`output_config.format`) 대신, 시스템 프롬프트로 "JSON만 출력하라"고 지시하고 응답을 `JSON.parse`하는 방식을 선택. 실제로 모델이 ```` ```json ```` 코드블록으로 감싸 응답하는 경우가 있어, 파싱 전에 코드블록 마커를 제거하는 처리를 추가함(검증됨).
- **PDF 미지원**: pdfplumber(Python) 대체재가 순수 브라우저 JS엔 없음. 파일 업로드 input은 UI에 남겨뒀지만 `disabled` 처리.

## 알려진 한계

- 근거성 검증이 없어 모델이 원문에 없는 내용을 인용문으로 제시해도 그대로 화면에 표시됨 (실제로 API 검증 중 "..."로 두 문장을 이어붙인 가짜 인용문 사례를 확인함 — 태스크 4로 해결 예정).
- 히스토리는 브라우저별 `localStorage`에도 남지만(오프라인 캐시용), Supabase `analyses` 익명 정책은 "user_id가 NULL인 행"을 모든 익명 사용자가 함께 CRUD할 수 있게 되어 있음 — 로그인 기능 전까지는 사실상 공개 데이터로 취급해야 함(민감 리포트 입력 주의).
- 이 프로젝트의 Supabase 익명 로그인(anonymous auth)은 비활성 상태 — 켜려면 대시보드에서 별도 설정 필요.

## 완료: UI/UX 개선 + 요약 불릿화 (2026-08-12)

- 헤더 로고에 아이콘(`findecode-icon.png`, 사용자 제공 `findecod_로고.png`에서 아이콘만 크롭 후 배경 투명화) 추가, 로고 클릭 시 입력 화면으로 이동.
- 소개 화면 본문 폰트 확대(15→17px) + 굵게, 핵심 키워드(쉬운 요약/키워드 설명/인사이트, 브라우저 비노출, 투자자문/팩트체크 제외)를 `--danger` 레드로 강조.
- 입력 화면에 어떤 문서가 적합한지 안내 문구 추가.
- 좌우 여백에 히스토리로 연결되는 카드(`.history-peek-card`)를 배치. 왼쪽 카드는 최근 분석 1건을 동적으로 미리보기(없으면 안내 문구). `max-width:1180px`에서 숨김.
- 결과 화면을 `min-width:900px`에서 3열 그리드로, 각 컬럼은 `max-height`+`overflow-y:auto`로 내부 스크롤(페이지 자체는 스크롤 불필요). 그 아래 폭에서는 기존처럼 세로로 쌓임.
- Edge Function 프롬프트의 `summary` 단일 문자열을 `summary_points`(3~6개 불릿) 배열로 변경, 결과 화면에 글머리 기호 리스트로 렌더링해 가독성 개선. DB `analyses.summary` 컬럼에는 줄바꿈으로 합쳐 저장(스키마 변경 없음).
- 모바일 네비게이션 탭이 4개(소개 추가)로 늘어나며 좁은 화면에서 `min-width:auto` 이슈로 밀릴 수 있어 `.nav-btn`에 `min-width:0` 방어 코드 추가(헤드리스 브라우저로 375px~390px 폭에서 실제 겹침/오버플로 없음을 좌표 측정으로 확인).

## 완료: Supabase 연동 + API 키 서버 이전 (2026-08-12)

- `analyses`/`analysis_keywords`/`analysis_insights` 테이블 생성(RLS 포함, 프로젝트 `samsung_ai`). 분석 완료마다 `app.js`가 Supabase에 결과를 적재. anon 키 경로로 insert/select 실사용 검증 완료.
- Anthropic API 키 브라우저 노출 문제 해결: Supabase Edge Function `analyze-report`를 만들어 Claude 호출을 서버 측으로 이전. 키는 Edge Function 시크릿(`ANTHROPIC_API_KEY`)에만 존재하고 브라우저는 절대 보지 못함. 설정 화면(API 키 입력 UI) 전체 제거.
- `app.js`의 `callClaudeAnalysis()`가 `fetch()` 직접 호출 대신 `supabaseClient.functions.invoke("analyze-report", ...)`를 사용하도록 변경.

## 완료: 디자인 시스템 적용

- 1차: Tesla 스타일 DESIGN.md 적용 → 사용자 피드백("너무 구림")으로 폐기.
- 2차: getdesign.md CLI(`npx getdesign@latest add notion`)로 **Notion 분석 DESIGN.md**를 받아 적용. 카드형(feature-card, 12px radius), Notion Blue(#0075de) 단일 accent, warm-canvas 배경(#f6f5f4)로 전체 재스타일링.
- 기회/리스크/참고 뱃지는 Notion의 "sticker palette는 상태 표시에만" 원칙에 따라 초록/주황/회색 텍스트색으로 복원.
- 참고: git 터미널에서 한글 파일명이 8진수로 깨져 보이는 문제는 `core.quotepath false` 설정으로 해결함(파일 자체 인코딩 문제 아님).

## 완료: API 키 설정 화면 분리 + PDF 업로드

- API 키 입력을 입력 화면에서 분리해 별도 "설정" 화면(4번째 탭)으로 이동. 분석 시 키가 없으면 자동으로 설정 화면으로 이동.
- PDF 업로드를 pdf.js(CDN, `jsdelivr.net/npm/pdfjs-dist@6.2.108`)로 구현. `app.js`를 `type="module"`로 변경해 `import` 사용. PDF 선택 시 텍스트를 추출해 텍스트 입력창에 채워줌 (스캔본은 추출 실패 안내).
- 문서가 너무 길면(4만자 초과) 앞부분만 잘라 분석하도록 클라이언트 가드 추가 (비용/응답시간 보호).
- 히스토리의 `sourceName`이 PDF 업로드 시 파일명을 반영하도록 개선.

## QA 분석 결과 (2026-08-12, 코드 변경 없는 분석 항목)

- **미해결 - 높음**: 근거성(hallucination) 검증 없음(태스크 4)
- **해결됨(2026-08-12)**: API 키 localStorage 평문 저장 → Supabase Edge Function으로 서버 이전
- **미해결 - 중간**: 히스토리 삭제 기능 없음, 429/529 재시도 로직 없음, 분석 중 텍스트 입력창 잠금 없음, 중복 분석 dedupe 없음
- **미해결 - 낮음**: `aria-live` 없음, fetch 타임아웃 없음, `escapeHtml`이 쌍따옴표 미처리
- **해결됨**: 문서 길이 가드 없음 → 4만자 초과 시 자동 절삭으로 해결. max_tokens 잘림 감지는 여전히 미해결(다음 항목).
