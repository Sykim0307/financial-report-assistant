# 기술 규칙

- 순수 HTML/CSS/JS로만 구현한다 (프레임워크·빌드 도구·서버 없음).
- 파일은 `index.html`, `style.css`, `app.js` 3개로 유지한다.
- 데이터는 브라우저 `localStorage`에 저장한다 (서버/DB 없음).

# 작업 규칙

- 표는 마크다운 표로 작성한다.
- 금액은 억원 단위로 표기한다.
- 결론을 가장 먼저 제시하고, 근거는 뒤에 정리한다.
- 수치를 제시할 때는 계산 근거(원본 값, 계산식, 출처/페이지 등)를 함께 표시한다.
- 한 번에 한 태스크만 진행한다.
- 변경 후에는 반드시 브라우저에서 확인한다.
